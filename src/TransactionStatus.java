package Stock_Trading_Platform.src;

public enum TransactionStatus {
    SUCCESS,
    USER_NOT_FOUND,
    STOCK_NOT_FOUND,
    INSUFFICIENT_BALANCE,
    INSUFFICIENT_STOCK,
    INVALID_QUANTITY,
    PORTFOLIO_NOT_FOUND,
    INSUFFICIENT_SHARES
}
